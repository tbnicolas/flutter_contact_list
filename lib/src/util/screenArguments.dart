import 'package:flutter_contact_list/src/model/contac_model.dart';

class ScreenArguments {
  final int tipo;
  final ContactListModel contactListModel;

  ScreenArguments(this.tipo, this.contactListModel);
}